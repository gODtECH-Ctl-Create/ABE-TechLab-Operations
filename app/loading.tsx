export default function Loading() {
  return (
    <main className="main">
      <section className="loading-stage" role="status" aria-live="polite" aria-label="Loading Operations workspace">
        <span>Loading Operations workspace…</span>
      </section>
    </main>
  );
}
